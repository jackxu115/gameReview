import {actionType} from "../Helper";

const initialState = {
    allGames: [],
}


export const gameReducer = (state = initialState, action ) => {
    switch (action.type) {
        default:
            return state
    }
}